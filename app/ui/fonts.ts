import { Inder, Lusitana } from 'next/font/google';

export const inter = Inder({
  subsets: ['latin'],
  weight: '400',
});

export const lusitana = Lusitana({
  subsets: ['latin'],
  weight: '400',
});
